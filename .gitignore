# ─────────────────────────────────────────────────────────────────────────────
# TODO lo sensible vive en UNA sola carpeta (memoria.md D12): el Excel crudo,
# los derivados, los modelos, los informes de EDA y el glosario/mapeo real.
# Una sola regla = una sola cosa que puede fallar. NUNCA se versiona (D5).
local_privado/
# ─────────────────────────────────────────────────────────────────────────────

# Red de seguridad: si alguien crea estas rutas FUERA de local_privado/, tampoco
# deben llegar a git. Antes vivian aqui como reglas principales (learn.md L3).
data/
models/
reports/
*.xlsx
*.xls
*.csv
*.parquet
*.pkl
*.joblib
glosario_sensibles.md
mapeo_sensibles.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/

# SO / editor
.DS_Store
.vscode/
