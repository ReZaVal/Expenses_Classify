# Datos crudos sensibles (data financiera real) — NUNCA se versionan
# (decisión cerrada: memoria.md D5, ex-P2).
data/raw/
data/processed/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/

# Informes de EDA: contienen etiquetas y valores reales
reports/

# Modelos y artefactos pesados
*.pkl
*.joblib
models/*.bin

# SO / editor
.DS_Store
.vscode/

# Data de entrenamiento y validación (si se genera)
.xlsx

# Glosario de seudónimos: llave real<->seudónimo, SOLO local (nunca a GitHub)
glosario_sensibles.md
# Mapa que consume el código (mismos valores reales, formato máquina)
mapeo_sensibles.json
